import {articles} from "./drivers/articles";
import {users} from "./drivers/users";
import {generals} from "./drivers/generals";
import {profiles} from "./drivers/profiles";

export const Api = {
  articles: articles,
  users: users,
  generals: generals,
  profiles: profiles,
};
